# AI Digest Feed

This repo hosts only the generated RSS feed (`feed.xml`) and episode MP3s
for the Daily AI News Digest podcast, published via GitHub Pages. It
contains no application source code — that lives in the private
`Idea-to-Ship` repo. Content here is generated/published by that repo's
pipeline (AIN-11).
